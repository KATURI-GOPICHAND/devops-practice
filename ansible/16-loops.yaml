- name: demo on loops
  hosts: local
  connection: local
  tasks: 
  - name: print names
    ansible.builtin.debug:
      msg: Hello "{{ item }}"   # item is reserved keyword
    loops:
    - Gopi
    - Chand
    - Gopichand
    - Rahul